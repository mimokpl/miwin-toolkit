// Code generated by protoc-gen-dart-http. DO NOT EDIT.

// ignore_for_file: unused_import, unnecessary_cast, avoid_print, non_constant_identifier_names, avoid_equals_and_hash_code_on_mutable_classes

import 'dart:async';
import 'dart:convert';
import 'dart:typed_data';
import '../../../../transport.dart';
import '../../../../proto_wire.dart';

/// EdgeRequest mixes a scalar field with repeated-message and message-valued
/// map fields. The message collections cannot be meaningfully serialized as
/// query parameters, so query routes must skip them and keep only the scalar.
class EdgeRequest {
  Map<String, Nested>? nestedMap;
  List<Nested>? repeatedNested;
  String? scalar;

  EdgeRequest({
    this.nestedMap,
    this.repeatedNested,
    this.scalar,
  });

  factory EdgeRequest.fromJson(Map<String, dynamic> json) {
    return EdgeRequest(
      nestedMap: (json['nestedMap'] as Map<String, dynamic>?)?.map((k, v) => MapEntry(k, Nested.fromJson(v as Map<String, dynamic>))),
      repeatedNested: (json['repeatedNested'] as List<dynamic>?)?.map((e) => Nested.fromJson(e as Map<String, dynamic>)).toList(),
      scalar: json['scalar'] as String?,
    );
  }

  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
    if (nestedMap != null) json['nestedMap'] = nestedMap!.map((k, v) => MapEntry(k, v.toJson()));
    if (repeatedNested != null) json['repeatedNested'] = repeatedNested!.map((e) => e.toJson()).toList();
    if (scalar != null) json['scalar'] = scalar;
    return json;
  }

  @override
  String toString() {
    return 'EdgeRequest(nestedMap: $nestedMap, repeatedNested: $repeatedNested, scalar: $scalar)';
  }

  @override
  bool operator ==(Object other) =>
    identical(this, other) ||
    other is EdgeRequest &&
      runtimeType == other.runtimeType
      && nestedMap == other.nestedMap
      && repeatedNested == other.repeatedNested
      && scalar == other.scalar
    ;

  @override
  int get hashCode => Object.hashAll([
    nestedMap,
    repeatedNested,
    scalar,
  ]);

  EdgeRequest copyWith({
    Map<String, Nested>? nestedMap,
    List<Nested>? repeatedNested,
    String? scalar,
  }) {
    return EdgeRequest(
      nestedMap: nestedMap ?? this.nestedMap,
      repeatedNested: repeatedNested ?? this.repeatedNested,
      scalar: scalar ?? this.scalar,
    );
  }

  /// 二进制 proto 编码（MQTT 等二进制传输用；字段按编号升序写出）
  Uint8List writeToBuffer() {
    final w = ProtoWireWriter();
    _writeTo(w);
    return w.toBuffer();
  }

  void _writeTo(ProtoWireWriter w) {
    // field 1: scalar
    final f1 = scalar;
    if (f1 != null) {
      w.writeString(1, f1);
    }
    // field 2: repeatedNested
    final f2 = repeatedNested;
    if (f2 != null) {
      for (final e in f2) { final cw = ProtoWireWriter(); e._writeTo(cw); w.writeRaw(2, cw.toBuffer()); }
    }
    // field 3: nestedMap
    final f3 = nestedMap;
    if (f3 != null) {
      f3.forEach((k, v) {
        final ew = ProtoWireWriter();
        ew.writeString(1, k);
        final vw = ProtoWireWriter(); v._writeTo(vw); ew.writeRaw(2, vw.toBuffer());
        w.writeRaw(3, ew.toBuffer());
      });
    }
  }

  factory EdgeRequest.fromBuffer(List<int> bytes) {
    return EdgeRequest._readFrom(ProtoWireReader(bytes));
  }

  static EdgeRequest _readFrom(ProtoWireReader r) {
    final m = EdgeRequest();
    while (r.next()) {
      switch (r.fieldNumber) {
        // field 1: scalar
        case 1: {
          m.scalar = r.readString();
          break;
        }
        // field 2: repeatedNested
        case 2: {
          m.repeatedNested = r.readNestedList().map(Nested._readFrom).toList();
          break;
        }
        // field 3: nestedMap
        case 3: {
          final er = r.readNested();
          String? k;
          Nested? v;
          while (er.next()) {
            switch (er.fieldNumber) {
              case 1: {
                k = er.readString();
                break;
              }
              case 2: {
                v = Nested._readFrom(er.readNested());
                break;
              }
              default: er.skip();
            }
          }
          if (k != null && v != null) {
            (m.nestedMap ??= {})[k] = v;
          }
          break;
        }
        default: {
          r.skip();
        }
      }
    }
    return m;
  }

}

class Nested {
  String? string;

  Nested({
    this.string,
  });

  factory Nested.fromJson(Map<String, dynamic> json) {
    return Nested(
      string: json['string'] as String?,
    );
  }

  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
    if (string != null) json['string'] = string;
    return json;
  }

  @override
  String toString() {
    return 'Nested(string: $string)';
  }

  @override
  bool operator ==(Object other) =>
    identical(this, other) ||
    other is Nested &&
      runtimeType == other.runtimeType
      && string == other.string
    ;

  @override
  int get hashCode => Object.hashAll([
    string,
  ]);

  Nested copyWith({
    String? string,
  }) {
    return Nested(
      string: string ?? this.string,
    );
  }

  /// 二进制 proto 编码（MQTT 等二进制传输用；字段按编号升序写出）
  Uint8List writeToBuffer() {
    final w = ProtoWireWriter();
    _writeTo(w);
    return w.toBuffer();
  }

  void _writeTo(ProtoWireWriter w) {
    // field 1: string
    final f1 = string;
    if (f1 != null) {
      w.writeString(1, f1);
    }
  }

  factory Nested.fromBuffer(List<int> bytes) {
    return Nested._readFrom(ProtoWireReader(bytes));
  }

  static Nested _readFrom(ProtoWireReader r) {
    final m = Nested();
    while (r.next()) {
      switch (r.fieldNumber) {
        // field 1: string
        case 1: {
          m.string = r.readString();
          break;
        }
        default: {
          r.skip();
        }
      }
    }
    return m;
  }

}

class EdgeServiceClient {
  final ClientTransport _transport;

  EdgeServiceClient(this._transport);

  /// Query route whose request mixes a scalar field (the only query parameter
  /// that should be emitted) with repeated-message and message-valued map
  /// fields that must be skipped.
  Future<EdgeRequest> queryWithMessageCollections(EdgeRequest request, {Map<String, String>? headers}) async {
    final path = '/v1/edge';
    final queryParams = <String>[];
    if (request.scalar != null) {
      queryParams.add('scalar=${Uri.encodeComponent(request.scalar!.toString())}');
    }
    var uri = path;
    if (queryParams.isNotEmpty) {
      uri += '?${queryParams.join("&")}';
    }
    final result = await _transport.unary(uri, 'GET', null, TransportMeta(
      service: 'EdgeService',
      method: 'QueryWithMessageCollections',
    ), headers: headers);
    return EdgeRequest.fromJson(result as Map<String, dynamic>);
  }

  /// Star body with a path-bound field: the field arrives via the URL path and
  /// must be removed from the JSON body to avoid a binding conflict on the
  /// server's synthetic oneof.
  Future<EdgeRequest> starBodyWithPath(EdgeRequest request, {Map<String, String>? headers}) async {
    if (request.scalar == null) {
      throw ArgumentError('missing required field request.scalar');
    }
    final path = '/v1/edge/${request.scalar}:starBodyWithPath';
    final bodyMap = request.toJson();
    bodyMap.remove('scalar');
    final body = jsonEncode(bodyMap);
    final result = await _transport.unary(path, 'POST', body, TransportMeta(
      service: 'EdgeService',
      method: 'StarBodyWithPath',
    ), headers: headers);
    return EdgeRequest.fromJson(result as Map<String, dynamic>);
  }

  /// Path literal and verb containing a dollar sign: Dart interpolates $ in
  /// every string form, so both must be escaped when emitted into the
  /// generated client, while the variable segment stays an interpolation.
  Future<EdgeRequest> dollarLiteral(EdgeRequest request, {Map<String, String>? headers}) async {
    if (request.scalar == null) {
      throw ArgumentError('missing required field request.scalar');
    }
    final path = '/v1/li\$teral/${request.scalar}:dollar\$verb';
    final result = await _transport.unary(path, 'GET', null, TransportMeta(
      service: 'EdgeService',
      method: 'DollarLiteral',
    ), headers: headers);
    return EdgeRequest.fromJson(result as Map<String, dynamic>);
  }
}

class ApiClient {
  final ClientTransport _transport;

  EdgeServiceClient? _edgeService;

  ApiClient(this._transport);

  EdgeServiceClient get edgeService {
    _edgeService ??= EdgeServiceClient(_transport);
    return _edgeService!;
  }

  /// Closes all service clients and releases resources.
  void dispose() {
    _edgeService = null;
  }
}

ApiClient createApiClient(ClientTransport transport) {
  return ApiClient(transport);
}


// @@protoc_insertion_point(dart-http-eof)
