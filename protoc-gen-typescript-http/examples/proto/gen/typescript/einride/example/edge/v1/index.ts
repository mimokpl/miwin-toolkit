// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable no-use-before-define, @typescript-eslint/no-unused-vars, prettier/prettier */
// @ts-nocheck: generated code does not require type checking

interface TransportMeta {
  service: string;
  method: string;
}

export interface ClientTransport {
  unary(
    path: string,
    method: string,
    body: null | string,
    meta: TransportMeta,
  ): Promise<unknown>;
  serverStream<T>(path: string, meta: TransportMeta): ServerStream<T>;
  duplexStream<TIn, TOut>(path: string, meta: TransportMeta): DuplexStream<TIn, TOut>;
}

export interface ServerStream<T> {
  onEvent(listener: (data: T) => void): () => void;
  onError(handler: (error: Error) => void): void;
  close(): void;
}

export interface DuplexStream<TIn, TOut> extends ServerStream<TOut> {
  send(data: TIn): void;
}

// EdgeRequest mixes a scalar field with repeated-message and message-valued
// map fields. The message collections cannot be meaningfully serialized as
// query parameters, so query routes must skip them and keep only the scalar.
export type EdgeRequest = {
  nestedMap: { [key: string]: Nested } | undefined;
  repeatedNested: Nested[] | undefined;
  scalar: string | undefined;
};

export type Nested = {
  string: string | undefined;
};

export interface EdgeService {
  // Query route whose request mixes a scalar field (the only query parameter
  // that should be emitted) with repeated-message and message-valued map
  // fields that must be skipped.
  QueryWithMessageCollections(
    request: EdgeRequest,
  ): Promise<EdgeRequest>;
  // Star body with a path-bound field: the field arrives via the URL path and
  // must be removed from the JSON body to avoid a binding conflict on the
  // server's synthetic oneof.
  StarBodyWithPath(
    request: EdgeRequest,
  ): Promise<EdgeRequest>;
  // Path literal and verb containing a dollar sign: Dart interpolates $ in
  // every string form, so both must be escaped when emitted into the
  // generated client, while the variable segment stays an interpolation.
  DollarLiteral(
    request: EdgeRequest,
  ): Promise<EdgeRequest>;
}

export function createEdgeServiceClient(
  transport: ClientTransport,
): EdgeService {
  return {
    QueryWithMessageCollections(request) {
      const path = `/v1/edge`;
      const body = null;
      const queryParams: string[] = [];
      if (request.scalar) {
        queryParams.push(
          `scalar=${encodeURIComponent(request.scalar.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'EdgeService',
        method: 'QueryWithMessageCollections',
      }) as Promise<EdgeRequest>;
    },
    StarBodyWithPath(request) {
      if (request.scalar === undefined || request.scalar === null) {
        throw new Error('missing required field request.scalar');
      }
      const path = `/v1/edge/${request.scalar}:starBodyWithPath`;
      const bodyMap = { ...request } as Record<string, unknown>;
      delete bodyMap["scalar"];
      const body = JSON.stringify(bodyMap);
      return transport.unary(path, 'POST', body, {
        service: 'EdgeService',
        method: 'StarBodyWithPath',
      }) as Promise<EdgeRequest>;
    },
    DollarLiteral(request) {
      if (request.scalar === undefined || request.scalar === null) {
        throw new Error('missing required field request.scalar');
      }
      const path = `/v1/li$teral/${request.scalar}:dollar$verb`;
      const body = null;
      return transport.unary(path, 'GET', body, {
        service: 'EdgeService',
        method: 'DollarLiteral',
      }) as Promise<EdgeRequest>;
    },
  };
}
export class ApiClient {
  private _edgeService?: EdgeService;
  private readonly _transport: ClientTransport;

  constructor(transport: ClientTransport) {
    this._transport = transport;
  }

  get edgeService(): EdgeService {
    return this._edgeService ??= createEdgeServiceClient(this._transport);
  }
}

export function createApiClient(transport: ClientTransport): ApiClient {
  return new ApiClient(transport);
}


// @@protoc_insertion_point(typescript-http-eof)
